---
import DocLayout from "../../layouts/DocLayout.astro";
import ComponentSection from "../../components/docs/ComponentSection.astro";
import ComponentExample from "../../components/docs/ComponentExample.astro";
import CodeBlock from "../../components/docs/CodeBlock.astro";
import PropsTable from "../../components/docs/PropsTable.astro";
import {
  BannerVariantsDemo,
  BannerDefaultDemo,
  BannerAlertDemo,
  BannerErrorDemo,
  BannerWithIconDemo,
  BannerCustomContentDemo,
} from "../../components/demos/BannerDemo";
---

<DocLayout
  title="Banner"
  description="Displays contextual inline messages for informational, alert, or error states."
  sourceFile="components/banner"
  storybookPath="story/components-banner"
>
  <!-- Demo -->
  <ComponentSection>
    <ComponentExample
      code={`<Banner>This is an informational banner.</Banner>
<Banner variant={BannerVariant.ALERT}>
  This is an alert banner.
</Banner>
<Banner variant={BannerVariant.ERROR}>
  This is an error banner.
</Banner>`}
    >
      <BannerVariantsDemo client:visible />
    </ComponentExample>
  </ComponentSection>

  <!-- Installation -->
  <ComponentSection>
    <h2 class="mb-4 text-2xl font-bold">Installation</h2>
    <CodeBlock code={`npm install @cloudflare/kumo`} lang="bash" />
  </ComponentSection>

  <!-- Usage -->
  <ComponentSection>
    <h2 class="mb-4 text-2xl font-bold">Usage</h2>
    <CodeBlock
      code={`import { Banner, BannerVariant } from "@cloudflare/kumo";
import { Info } from "@phosphor-icons/react";

export default function Example() {
  return (
    <Banner icon={<Info />} variant={BannerVariant.DEFAULT}>
      You have a new message.
    </Banner>
  );
}`}
      lang="tsx"
    />
  </ComponentSection>

  <!-- Examples -->
  <ComponentSection>
    <h2 class="mb-6 text-2xl font-bold">Examples</h2>

    <!-- Variants -->
    <div class="mb-12">
      <h3 class="mb-4 text-xl font-semibold">Variants</h3>
      <div class="space-y-8">
        <div>
          <h4 class="mb-3 text-base font-medium">Default</h4>
          <ComponentExample
            code={`<Banner>This is an informational banner.</Banner>`}
          >
            <BannerDefaultDemo client:visible />
          </ComponentExample>
        </div>

        <div>
          <h4 class="mb-3 text-base font-medium">Alert</h4>
          <ComponentExample
            code={`<Banner variant={BannerVariant.ALERT}>
  Your session will expire soon.
</Banner>`}
          >
            <BannerAlertDemo client:visible />
          </ComponentExample>
        </div>

        <div>
          <h4 class="mb-3 text-base font-medium">Error</h4>
          <ComponentExample
            code={`<Banner variant={BannerVariant.ERROR}>
  We couldn't save your changes.
</Banner>`}
          >
            <BannerErrorDemo client:visible />
          </ComponentExample>
        </div>
      </div>
    </div>

    <!-- With icon -->
    <div class="mb-12">
      <h3 class="mb-4 text-xl font-semibold">With icon</h3>
      <ComponentExample
        code={`<Banner icon={<WarningCircle />} variant={BannerVariant.ALERT}>
  Review your billing information.
</Banner>`}
      >
        <BannerWithIconDemo client:visible />
      </ComponentExample>
    </div>

    <!-- Custom content -->
    <div class="mb-12">
      <h3 class="mb-4 text-xl font-semibold">Custom content</h3>
      <ComponentExample
        code={`<Banner icon={<Info />}>
  <Text DANGEROUS_className="text-inherit">
    This banner supports <strong>custom content</strong> with Text.
  </Text>
</Banner>`}
      >
        <BannerCustomContentDemo client:visible />
      </ComponentExample>
    </div>
  </ComponentSection>

  <!-- API Reference -->
  <ComponentSection>
    <h2 class="mb-4 text-2xl font-bold">API Reference</h2>
    <PropsTable component="Banner" />
  </ComponentSection>
</DocLayout>
