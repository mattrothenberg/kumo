import type { ReactNode } from "react";
import { cn } from "../../utils/cn";

export interface {{pascalCase name}}Props {
  className?: string;
  children?: ReactNode;
}

/**
 * {{pascalCase name}} - A layout component for page composition
 * 
 * Layouts are page-level components that provide consistent structure
 * for common page patterns like resource lists, dashboards, and settings.
 */
export function {{pascalCase name}}({ className, children }: {{pascalCase name}}Props) {
  return (
    <div className={cn(
      "{{kebabCase name}}",
      className
    )}>
      {children}
    </div>
  );
}
