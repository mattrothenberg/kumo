import type { ReactNode } from "react";
import { cn } from "../../utils/cn";

export interface {{pascalCase name}}Props {
  className?: string;
  children?: ReactNode;
}

/**
 * {{pascalCase name}} - A block-level component for page composition
 * 
 * Blocks are higher-level components that compose multiple base components
 * to create common page patterns and layouts.
 */
export function {{pascalCase name}}({ className, children }: {{pascalCase name}}Props) {
  return (
    <div className={cn(
      "{{kebabCase name}}",
      className
    )}>
      {children}
    </div>
  );
}
