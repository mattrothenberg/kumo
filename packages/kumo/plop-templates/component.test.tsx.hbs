import { describe, it, expect } from 'vitest';
import { {{pascalCase name}} } from './{{kebabCase name}}';

describe('{{pascalCase name}}', () => {
  it('should be importable', () => {
    expect({{pascalCase name}}).toBeDefined();
    expect(typeof {{pascalCase name}}).toBe('function');
  });

  it('should have correct display name', () => {
    expect({{pascalCase name}}.name).toBe('{{pascalCase name}}');
  });

  // TODO: Add component-specific tests here
  // Example:
  // it('should render with props', () => {
  //   const props = { className: 'test' };
  //   // Add your test implementation
  // });
});
